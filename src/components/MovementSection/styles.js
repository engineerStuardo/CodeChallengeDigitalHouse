import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  list: {
    height: 360,
    backgroundColor: '#fff',
    borderRadius: 10,
  },
  title: {
    fontSize: 14,
    fontWeight: '800',
    color: '#9B9898',
    marginBottom: 20,
    marginTop: 20,
  },
});
